# 🚀 クイックスタートガイド

音ゲー広辞苑を素早く起動するための簡単な手順です。

## ⚡ 最短起動手順

### 1. 依存関係のインストール
```bash
npm install
```

### 2. アプリケーション起動
```bash
npm start
```

### 3. ブラウザでアクセス
起動後、以下のURLにアクセス：
- http://localhost:3000

## 📋 起動確認チェックリスト

- [ ] Node.js (v16以上) がインストールされている
- [ ] `npm install` が正常に完了した
- [ ] `npm start` でエラーが発生していない
- [ ] ブラウザで http://localhost:3000 にアクセスできる
- [ ] ホーム画面が正常に表示される

## 🔧 トラブル時の対処法

### ポート3000が使用中の場合
```bash
# 別のポートで起動
PORT=3001 npm start
```

### 依存関係のエラーが発生した場合
```bash
# キャッシュをクリアして再インストール
rm package-lock.json
rm -rf node_modules
npm install
```

### PowerShellで`&&`エラーが発生した場合
コマンドを分けて実行：
```bash
npm install
npm start
```

## 📱 基本的な使い方

1. **ホーム画面**: アプリの概要を確認
2. **「楽曲を探す」ボタン**: 楽曲検索ページへ移動
3. **検索バー**: 楽曲名やアーティスト名で検索
4. **フィルタ**: ゲーム別・難易度別で絞り込み
5. **楽曲詳細**: 楽曲をクリックして詳細情報を表示

## 🆘 サポート

問題が解決しない場合は、詳細な `README.md` を参照するか、開発者にお問い合わせください。 